const first = "Hello, ";
const second = "world!";

// 1) Plus operator
const combined1 = first + second; // "Hello, world!"

// 2) Template literal (preferred when you need interpolation)
const combined2 = `${first}${second}`; // "Hello, world!"
