const first = "Hello";
const last  = "World";
const full  = first + " " + last;   // "Hello World"
const first = "Hello";
const last  = "World";
const full  = `${first} ${last}`;   // "Hello World"
const full = "Hello".concat(" ", "World");
