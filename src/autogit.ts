const first = "Hello";
const last  = "World";

// 1. Plus operator
const combined1 = first + " " + last;   // "Hello World"

// 2. Template literal (preferred for readability)
const combined2 = `${first} ${last}`;     // "Hello World"
