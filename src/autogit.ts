const first = "Hello";
const second = "World";
const result = first + " " + second;   // "Hello World"
const result = `${first} ${second}`;   // "Hello World"
const result = first.concat(" ", second);
